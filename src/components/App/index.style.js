import { StyleSheet }from 'react-native';
import {
  STATUS_BAR_HEIGHT,
} from '../../utils';

export default StyleSheet.create({
  container: {
    marginTop: STATUS_BAR_HEIGHT,
    backgroundColor: '#fff',
  },
});

