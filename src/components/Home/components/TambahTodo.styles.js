import { StyleSheet } from 'react-native';
import{
  DEVICE_WIDTH,
  DEVICE_HEIGHT,
} from '../../../utils';

export default StyleSheet.create({
  inputan: {
    height: 50,
    width: DEVICE_WIDTH,
    backgroundColor: 'gray',
  }
});

